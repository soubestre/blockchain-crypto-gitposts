## Error Type
Build Error

## Error Message
Ecmascript file had an error

## Build Output
./src/app/api/git-posts/route.ts:281:11
Ecmascript file had an error
  279 |
  280 |     // Use server client with proper session cookies - RLS will handle permissions
> 281 |     const supabase = await createClient(request);
      |           ^^^^^^^^
  282 |
  283 |     // Use centralized service to prepare post data with correct post_author_id
  284 |     const postData = await preparePostDataServer(

the name `supabase` is defined multiple times

Next.js version: 16.0.1 (Turbopack)
## Error Type
Build Error

## Error Message
Ecmascript file had an error

## Build Output
./src/app/api/git-posts/route.ts:281:11
Ecmascript file had an error
  279 |
  280 |     // Use server client with proper session cookies - RLS will handle permissions
> 281 |     const supabase = await createClient(request);
      |           ^^^^^^^^
  282 |
  283 |     // Use centralized service to prepare post data with correct post_author_id
  284 |     const postData = await preparePostDataServer(

the name `supabase` is defined multiple times

Next.js version: 16.0.1 (Turbopack)
## Error Type
Build Error

## Error Message
Ecmascript file had an error

## Build Output
./src/app/api/git-posts/route.ts:281:11
Ecmascript file had an error
  279 |
  280 |     // Use server client with proper session cookies - RLS will handle permissions
> 281 |     const supabase = await createClient(request);
      |           ^^^^^^^^
  282 |
  283 |     // Use centralized service to prepare post data with correct post_author_id
  284 |     const postData = await preparePostDataServer(

the name `supabase` is defined multiple times

Next.js version: 16.0.1 (Turbopack)
## Error Type
Build Error

## Error Message
Ecmascript file had an error

## Build Output
./src/app/api/git-posts/route.ts:281:11
Ecmascript file had an error
  279 |
  280 |     // Use server client with proper session cookies - RLS will handle permissions
> 281 |     const supabase = await createClient(request);
      |           ^^^^^^^^
  282 |
  283 |     // Use centralized service to prepare post data with correct post_author_id
  284 |     const postData = await preparePostDataServer(

the name `supabase` is defined multiple times

Next.js version: 16.0.1 (Turbopack)